---
- name: Injecting pre config vars
  include_vars:
    name: pre_config_vars
    file: vars.yml

- name: Creating projects folder
  file:
    name: "{{ pre_config_vars.project_path }}"
    state: directory
    owner: "{{ my_username }}"
    group: "{{ my_main_group }}"
